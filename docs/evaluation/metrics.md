# Check precision and recall

Generated by `scripts/measure_metrics.py` — do not edit. Regenerate with `make metrics`.

Data: synthetic clean bars from `tests/support/synth.py`, broken by the faults in
`tests/support/faults.py`. Corpus-independent — nothing under `data/` or
`tests/fixtures/` is read, so these numbers do not move when the sample does.

Scenarios: 29 (4 clean, 25 faulted), covering 19 fault types and 14 checks.

## Per check

Summed over every scenario in which the check ran, clean scenarios included.

| check | scenarios | TP | FP | FN | precision | recall | F1 | severity misses | row-id misses |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `ambiguous_local_time` | 23 | 4 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `duplicate_timestamp` | 29 | 10 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `intrabar_gap` | 23 | 39 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `invalid_ohlc` | 29 | 5 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `malformed_record` | 29 | 3 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `missing_session` | 29 | 3 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `missing_value` | 29 | 3 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `negative_volume` | 29 | 2 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `non_positive_price` | 29 | 1 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `off_grid_timestamp` | 23 | 1 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `outlier_return` | 29 | 2 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `stale_bar` | 29 | 5 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `timestamp_out_of_range` | 29 | 1 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |
| `zero_volume_with_range` | 29 | 1 | 0 | 0 | 1.000 | 1.000 | 1.000 | 0 | 0 |

## Where the matrix is not 1.000

Nothing: every check matched its declaration on all four axes, in every
scenario it ran in.

## Clean-data guard

Findings produced on perfectly clean synthetic bars. Every cell must be 0: a check
that fires here has precision 0 however good its recall, and will bury a real user
long before it helps them. `—` means the check does not run at that frequency.

| check | clean_minute (360 bars) | clean_full_sessions (2,760 bars) | clean_two_contracts (480 bars) | clean_daily (3 bars) |
|---|---:|---:|---:|---:|
| `ambiguous_local_time` | 0 | 0 | 0 | — |
| `duplicate_timestamp` | 0 | 0 | 0 | 0 |
| `intrabar_gap` | 0 | 0 | 0 | — |
| `invalid_ohlc` | 0 | 0 | 0 | 0 |
| `malformed_record` | 0 | 0 | 0 | 0 |
| `missing_session` | 0 | 0 | 0 | 0 |
| `missing_value` | 0 | 0 | 0 | 0 |
| `negative_volume` | 0 | 0 | 0 | 0 |
| `non_positive_price` | 0 | 0 | 0 | 0 |
| `off_grid_timestamp` | 0 | 0 | 0 | — |
| `outlier_return` | 0 | 0 | 0 | 0 |
| `stale_bar` | 0 | 0 | 0 | 0 |
| `timestamp_out_of_range` | 0 | 0 | 0 | 0 |
| `zero_volume_with_range` | 0 | 0 | 0 | 0 |

## Scenarios

| scenario | frequency | faults | expected | result | why it is here |
|---|---|---|---|---|---|
| `clean_minute` | minute | — | nothing | exact | Three dense ESH26 sessions. Any finding at all here is a false positive. |
| `clean_full_sessions` | minute | — | nothing | exact | Two complete 1,380-bar CME sessions across the 2026-03-08 spring forward. |
| `clean_two_contracts` | minute | — | nothing | exact | Two contracts, so `missing_session` has siblings to corroborate against. |
| `clean_daily` | daily | — | nothing | exact | Three clean daily bars — the daily half of the precision guard. |
| `exact_duplicate` | minute | `ExactDuplicate(n=2)` | `duplicate_timestamp` 4 @ INFO | exact | A re-sent file. Nothing is contradicted, so INFO is the correct severity. |
| `conflicting_duplicate` | minute | `ConflictingDuplicate()` | `duplicate_timestamp` 2 @ ERROR | exact | Same instant, different volume — contradictory, and therefore ERROR. |
| `swap_high_low` | minute | `SwapHighLow(rows=[10])` | `invalid_ohlc` 1 @ ERROR | exact | `high < low`: the one OHLC clause the real corpus never exhibits. |
| `close_outside_range` | minute | `CloseOutsideRange(rows=[11])` | `invalid_ohlc` 1 @ ERROR | exact | `high < max(open, close)` — 24 of the corpus's 43 genuine violations. |
| `negative_price` | minute | `NegativePrice(rows=[12])` | `non_positive_price` 1 @ ERROR | exact | `low` driven below zero on a non-exempt root (CL is exempt by config). |
| `negative_volume` | minute | `NegativeVolume(rows=[13])` | `negative_volume` 1 @ ERROR | exact | Never a market event, always a defect. |
| `null_close` | minute | `NullField(column='close', rows=[14])` | `missing_value` 1 @ ERROR | exact | An unparseable cell. Ingestion keeps the row so the user sees it in context. |
| `null_volume` | minute | `NullField(column='volume', rows=[15])` | `missing_value` 1 @ ERROR | exact | The same defect in a non-price column. |
| `zero_volume_with_range` | minute | `ZeroVolumeWithRange(rows=[18])` | `zero_volume_with_range` 1 @ WARNING | exact | Price moved with no trades: a contradiction with no benign reading. |
| `flat_zero_volume_bar` | minute | `FlatZeroVolumeBar(rows=[19])` | `stale_bar` 1 @ WARNING | exact | The shape of all 14,152 no-range bars in the corpus, inside an ACTIVE session. |
| `price_spike` | minute | `PriceSpike(row=60)` | `outlier_return` 2 @ INFO | exact | Two outliers, not one: the move into the spike and the move back out. |
| `off_grid_seconds` | minute | `OffGridSeconds(rows=[16])` | `off_grid_timestamp` 1 @ WARNING | exact | Minute bars nudged off the minute boundary, as a mislabelled tick feed would. |
| `future_timestamp` | minute | `FutureTimestamp(rows=[17])` | `timestamp_out_of_range` 1 @ WARNING | exact | A mis-scaled epoch, landing bars forty years out. |
| `ambiguous_local_time` | minute | `AmbiguousLocalTime(passes=2)` | `ambiguous_local_time` 4 @ WARNING, `duplicate_timestamp` 4 @ INFO, `stale_bar` 4 @ INFO | exact | The repeated fall-back hour, delivered twice — legitimately two checks. |
| `nonexistent_local_time` | minute | `NonexistentLocalTime()` | `malformed_record` 1 @ ERROR | exact | 02:30 on 2026-03-08 never happened; it reaches the engine as a reject. |
| `malformed_line` | minute | `MalformedLine(row_ids=[900100, 900101])` | `malformed_record` 2 @ ERROR | exact | A CSV line that never parsed into fields, also via the rejects frame. |
| `wall_clock_read_as_utc` | minute | `ShiftWallClockToUtc()` | nothing | exact | ADR-1's trap. Expects nothing: no row-level check can see it (see Definitions). |
| `intrabar_gap` | minute | `DropRange(start=2026-03-03 17:30:00, end=2026-03-03 18:09:00)` | `intrabar_gap` 39 @ WARNING | exact | A 39-minute hole inside a session that is still ACTIVE afterwards. |
| `missing_session_one_contract` | minute | `DropSession(session=2026-03-04, contract='ESH26')` | `missing_session` 1 @ WARNING | exact | One contract absent while its sibling traded: a hole in the feed, WARNING. |
| `missing_session_all_contracts` | minute | `DropSession(session=2026-03-04, severity=INFO)` | `missing_session` 2 @ INFO | exact | Every contract absent: a holiday, INFO. Same absence, different reading. |
| `swap_high_low_daily` | daily | `SwapHighLow(rows=[0])` | `invalid_ohlc` 1 @ ERROR | exact | The same OHLC clause at daily frequency. |
| `close_outside_range_daily` | daily | `CloseOutsideRange(rows=[2])` | `invalid_ohlc` 1 @ ERROR | exact | A settlement outside its own bar's range, in a traded session: ERROR. |
| `carried_forward_settlement` | daily | `CarriedForwardSettlement(rows=[1])` | `invalid_ohlc` 1 @ WARNING | exact | The corpus's 39-of-43 signature. Zero volume, so DORMANT, so WARNING. |
| `negative_volume_daily` | daily | `NegativeVolume(rows=[1])` | `negative_volume` 1 @ ERROR | exact | Negative volume on a daily bar. |
| `null_open_daily` | daily | `NullField(column='open', rows=[1])` | `missing_value` 1 @ ERROR | exact | A null price field on a daily bar. |

## Definitions

**The unit is one unit of `Expected.count` per check** — one bar, or for an absence
finding one missing minute or session. It is the unit `Expected` itself is written in and
the one `assert_findings_match` sums on, and it is the only unit two of the fourteen checks
have: `missing_session` names no rows, and `intrabar_gap`'s row ids are the two bracketing
survivors rather than the bars that vanished.

Per scenario and per check, with `want` the summed `Expected.count` and `got` the summed
`count` the check reported: **TP** = `min(want, got)`, **FP** = `max(0, got - want)`,
**FN** = `max(0, want - got)`. A check with findings and no expectation in that scenario is
a false positive for its whole output. A check reporting three bars where two were broken
scores TP 2 and FP 1 — it found the defect and over-reached, and the matrix says both.

**`severity misses`** counts expectations whose severity the check did not produce, and
**`row-id misses`** expected row ids missing from its evidence. Neither changes TP, FP or
FN: detection and triage are different failures, and a single number would hide which one
broke. A non-zero severity miss means the *regime model* mis-triaged a real detection.

**A fault with two legitimate consequences is scored twice, not penalised once.** An
ambiguous wall clock delivered twice arrives under `ambiguous="earliest"` with both copies
on the same instant, so it genuinely trips `ambiguous_local_time` and
`duplicate_timestamp`; `Expected` declares both and each is scored against its own want.

**`wall_clock_read_as_utc` expects nothing, and is here for that reason.** Reading a
Chicago wall clock as UTC leaves the frame internally consistent — coherent bars, unique
instants, dense sessions — so no row-level check can see it, and recall is not defined for
it. Only reconciliation between frequencies could catch it, and
`cross_frequency_mismatch` is a documented non-goal in `docs/ARCHITECTURE.md`. The
scenario is included so that limitation is visible in the numbers rather than absent
from them.

**How to read a precision below 1.000.** It means the check reported `count` no `Expected`
in that scenario declared. There are exactly two causes and they point in opposite
directions: the check over-reached, or the *fault* has a genuine consequence its
declaration omits — in which case the check was correct and the harness is understating
what the fault does. "Where the matrix is not 1.000" above names every instance;
`docs/EVALUATION.md` says which cause applies to each.

**Limitation: these faults are meant to be surgical.** Each is written to violate one
invariant and leave the others intact, which is what makes the precision half of the
assertion mean anything. Where the measurement shows one is not, that is itself reported
above rather than corrected out of the catalogue. Either way these figures say nothing
about compound defects — a feed outage that drops bars, stales the ones it keeps and
duplicates the recovery — and nothing about defect rates in real data. They measure
whether each check does its own job cleanly on a defect aimed at it.
